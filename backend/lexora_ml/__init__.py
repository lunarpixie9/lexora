"""Lexora ML: reading-level model trained on ASER, composite screening indicator,
and explanation helpers. Training scripts live in this package too so that
runtime and training share exactly the same feature code."""
