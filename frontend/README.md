# Lexora frontend

React 19 + TypeScript + Vite + Tailwind v4. See `../docs/SETUP.md` for setup and `../docs/ARCHITECTURE.md` for structure.

```bash
npm install
npm run dev      # http://localhost:5173 (proxies /api to http://localhost:8000)
npm run build    # type-check + production build
```
